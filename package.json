{
  "name": "idx-nego-alert",
  "version": "1.0.0",
  "private": true,
  "description": "Telegram alerts for large IDX negotiated-market transactions (>= Rp 50 bn per deal)",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\"",
    "start": "node src/main.js",
    "dry-run": "node src/main.js --dry-run"
  }
}
